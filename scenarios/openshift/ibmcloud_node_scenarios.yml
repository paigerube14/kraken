# yaml-language-server: $schema=../plugin.schema.json
- id: ibmcloud-node-reboot
  config:
    name: ""        
    label_selector: "node-role.kubernetes.io/master"    # When node_name is not specified, a node with matching label_selector is selected for node chaos scenario injection 
    runs: 1                             # Number of times to inject each scenario under actions (will perform on same node each time)                                                           
    instance_count: 1                   # Number of nodes to perform action/select that match the label selector                                             
    timeout: 45                         # Duration to wait for completion of node scenario injection
    skip_openshift_checks: False        # Set to True if you don't want to wait for the status of the nodes to change on OpenShift before passing the scenario 